name: Github test
run-name: waltur
on: [push]
jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test" 
      - run: cd insecure
      - run: npm install
      - run: npm audit
      - run: npm run lint
      
