name: Github test
run-name: waltur
on: [push]
jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test" 
      - run: npm audit insecure
      - run: npm run lint insecure
      
