name: Github test
run-name: ${{ github.actor }} 
on: [push]
jobs:
  Explore-Github-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test" 
      - run: npm audit
      - run: npm run lint
      
